/** Promedios mensuales de Tarija, Bolivia. Fallback cuando no hay datos climáticos reales. */
export declare const TARIJA_BY_MONTH: Record<number, {
    temperature: number;
    humidity: number;
    lightHours: number;
}>;
